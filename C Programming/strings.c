#include<stdio.h>
void milan(char kaka[]);
int main()
{
	char str[100],lola[100];
	scanf("%s",&str);
	milan(str);
}
	void milan(char kaka [])
	
	{
       char newstr[200];
		char salt[]="123";
		strcpy(newstr,ygfgjhjstr);
		strcat(newstr,salt);
		puts(newstr);
	}
	
	
